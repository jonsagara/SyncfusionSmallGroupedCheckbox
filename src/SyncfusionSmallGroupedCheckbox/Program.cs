using SyncfusionSmallGroupedCheckbox;

// The method will save the filled PDF to disk and launch it in Chrome.
const string templateFilePath = @"C:\Users\Jon\Desktop\wa_state_firearm_transfer_app.pdf";
SetGroupedCheckboxHelper.SetGroupedCheckbox(templateFilePath);
